# Create the documentdb-local system user for appliance-managed PostgreSQL clusters.
# No login shell; home is the top-level data directory.
u documentdb-local - "DocumentDB Local cluster owner" /var/lib/documentdb-local /usr/sbin/nologin
