schema_version: 2
# The suite commit is the only pin. Both ways of running the suite are derived
# from it: setup_functional_tests.sh checks this commit out, and the runner
# resolves the published test image built from it (tag sha-<short7>). Deriving
# rather than recording a second reference is what stops the two from naming
# different suite versions.
source_ref: documentdb/functional-tests@main
source_sha: 935605abe70b8f0230ab9a3b3767608e1d57a283
updated_by: guanzhousong
